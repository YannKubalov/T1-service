spring:
  datasource:
    url: jdbc:postgresql://localhost:1/postgres
    username: postgres
    password: password
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true